# DAR Page: Add Controlled Tier

X Expose `accessTiersVisibleToUsers` to UI.
- Add a placeholder to DAR Page for Step 3 CT. It should only show when `accessTiersVisibleToUsers` includes the Controlled Tier.