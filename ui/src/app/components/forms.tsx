
import {withStyle} from 'app/utils/index';

export const styles = {
  formSection: {
    marginTop: '1rem',
    minWidth: '29rem'
  }
};

export const FormSection = withStyle(styles.formSection)('div');
