import { IntegerValidatorDirective } from './integer-validator.directive';

describe('IntegerValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new IntegerValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
