version: "3.4"

services:
  db:
    platform: linux/amd64
    image: mysql:5.7.27
    env_file:
      - db/vars.env
    volumes:
      - db:/var/lib/mysql
    ports:
      - 127.0.0.1:3306:3306

volumes:
  db:
