version: "3.4"

services:
  db:
    image: mysql:8.0.26
    platform: linux/x86_64
    env_file:
      - db/vars.env
    volumes:
      - db:/var/lib/mysql
    ports:
      - 127.0.0.1:3306:3306

volumes:
  db:
