version: "3.4"

services:
  db:
    image: mariadb:10.3
    command: --default-authentication-plugin=mysql_native_password
    env_file:
      - db/vars.env
    volumes:
      - db:/var/lib/mysql
    ports:
      - 127.0.0.1:3306:3306
volumes:
  db:
