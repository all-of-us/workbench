buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'org.liquibase:liquibase-gradle-plugin:1.2.4'
        classpath 'mysql:mysql-connector-java:5.1.37'
    }
}
apply plugin: 'org.liquibase.gradle'

liquibase {
    activities {
        main {
            changeLogFile 'changelog/db.changelog-master.xml'
            url 'jdbc:mysql://db/workbench'
            // TODO(dmohs): Change this back to liquibase!
            username 'workbench'
            password 'workbench!pwd'
        }
    }
}
